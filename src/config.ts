const config = {port: 3000, host: 'localhost', exhangeServiceURL:'https://api.exchangerate-api.com/v4/latest/', supportedCurrencies: ['USD', 'EUR', 'GBP', 'ILS'], roundingPolicy: 0, lruCacheSize: 10};

export { config };